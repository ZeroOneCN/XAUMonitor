# XAUMonitor 策略优化方案（Roadmap）

> 2026-09-18 · 供决策 · 建议按优先级推进

---

## 结论先行

当前系统能"发信号"，但你**不知道这些信号赚不赚钱**——策略的「优势(edge)」完全未经验证。
在验证之前调参，等于蒙眼开车。所以排序是：

1. 🥇 **建立验证闭环**（先知道胜率，再谈优化）
2. 🥈 **风控与执行层**（历史两次爆仓的病因都在这里，不在信号）
3. 🥉 **信号逻辑精修**（有数据支撑后再做）

---

## 第一优先级：建立验证闭环

### 1.1 信号结果追踪（outcome tracking）★ 最基础
- **现状**：`signals.db` 只记录"发出了什么信号"，没记录"后来怎么走的"。
- **方案**：信号发出后持续跟踪后续 K 线，记录：
  - 先触及 **SL / TP1 / TP2 / 超时**
  - 最终 **R 倍数**（盈亏比）
  - 出结果用了多少根 K 线
- **产出**：每笔信号的结局 → 可算 **胜率 / 期望值(R) / 盈亏比 / 最大连亏**
- 没有这个，后面全是空谈。

### 1.2 回测框架
- 用 `calc_signals()` 跑历史数据（Twelve Data 单次 1000 根，可分段拉取累积数年）
- 模拟每笔信号的 TP/SL 触发顺序，扣除滑点与点差
- **输出指标**：胜率、期望值(R)、盈亏因子、最大回撤、交易数
- **分组统计**：S级真的比B级好吗？回踩真的比突破强吗？哪个周期最有效？

### 1.3 参数敏感度与寻优
- 关键参数网格搜索：`trend_stability` / `breakout_dist_atr` / `breakout_consec` /
  `pullback_confirm_bars` / `sl_cushion` / `momentum_body_ratio`
- ⚠️ **必须 walk-forward**（分段优化 + 样本外验证），否则必然过拟合

---

## 第二优先级：风控与执行层

> 回顾两轮爆仓（$680→0、$1200→$93），病因都是 **"方向看对但仓位远超账户承受、反弹中死扛"**，
> 不是信号不准。信号再准，仓位错就是归零。**这一层比信号更重要。**

### 2.1 仓位计算器 ★ 直接针对病因 —— ✅ 已完成
- 信号里直接给出 **建议手数**：账户资金 × 单笔风险% ÷ (SL距离 × 合约盎司) ✅
- 例：账户 $5000，风险 1%（$50），SL 距离 7.23，黄金 1 手=100 盎司
  → 手数 = 50 / (7.23 × 100) = **0.07 手**（实际风险 $50.61 = 1.01%）
- 把"该下多少手"变成信号的一部分，**不再靠手感和情绪** ✅
- **新增「止损偏宽」告警**：按风险%算出的手数低于券商最小手数时，推送里明说
  （例：账户 $500 想按 1% 风险下单，0.0067 手 < 最小 0.01 手 → 直接告知）
- 配置：`account_equity` / `risk_per_trade_pct` / `contract_oz` / `min_lot` / `max_lot`
- 信号卡片升级为**做单卡片**：手数 + 风险金额 + 盈亏比 一屏可见

### 2.2 硬性风控闸门
- 单日最大亏损熔断（如 -3% 停当日）
- 最大同时持仓数
- 最大连亏后强制冷静期
- **关键：反弹中禁止加仓/扛单**（历史两次都是这么死的）

### 2.3 事件日历过滤 ★ 个性化
- 非农 / CPI / FOMC 等重磅数据前后 N 分钟不开新仓
- 你第一次爆仓正是"**非农 + 伊朗冲突插针**"——这类事件可提前规避
- 数据源：免费经济日历（ForexFactory 抓取等）

### 2.4 时段过滤
- 黄金亚洲盘流动性低、假突破多；伦敦/纽约盘更有效
- 可对低流动性时段的信号降权或过滤

---

## 第三优先级：信号逻辑精修

### 3.1 趋势强度过滤（而非仅排列）
- 现在只看 EMA 排列（快线在慢线上方）
- 建议加 **ADX 或 EMA 斜率阈值**，区分"真趋势"与"均线黏合震荡"

### 3.2 止损/止盈结构化
- 止损：用近端**摆动高低点(swing)**，而非固定 EMA/ATR
- 止盈：前高/前低 + **斐波那契扩展位**（你已掌握三线法中的斐波那契）

### 3.3 多周期共振升级为"过滤"（而非仅标记）
- 现在：各周期独立出信号，只加 🔥 标记
- 建议：低周期信号需**高周期同向**才推送（如 5m 信号需 1h 同向）

### 3.4 只做高等级信号
- 把 `min_signal_grade` 提到 `"A"`，把 B 级噪音挡在门外
- **先回测验证"A级是否真优于B级"再定**，别拍脑袋

### 3.5 数据源（已实测）
- 已确认：Twelve Data 对黄金**无成交量**，`GC=F` 也不被支持
- 若要真实成交量：需换数据源（COMEX 期货行情），当前动能代理已是可行替代

---

## 方法论警告（重要）

1. **样本量**：1000 根 K 线里只有几十个信号，统计意义弱，至少拉 2~3 年数据。
2. **过拟合**：参数越多越容易"拟合历史"，宁可参数少而稳健。
3. **滑点与点差**：回测必须扣除（黄金点差约 0.2~0.4 美元）。
4. **前视偏差**：确认K线/状态机时序必须严格只使用"已收盘"的K线。
5. **别急着上实盘**：先模拟盘跑 1~2 个月，验证信号与回测是否一致。

---

## 建议推进顺序

| 周 | 目标 | 交付 |
|----|------|------|
| 第 1 周 | 结果追踪 + 回测框架 | **第一个真实胜率数字** |
| 第 2 周 | 风控层（仓位计算 + 熔断 + 事件过滤） | 单笔风险可控 |
| 第 3 周 | 基于回测精修信号 + walk-forward 验证 | 参数有数据支撑 |
| 第 4 周+ | 模拟盘验证 | 上实盘前的最后一道闸 |
