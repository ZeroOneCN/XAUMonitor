# 敏感/私有文件（不含密钥才可上传）
dpb_config.json
dpb_state.json

# 运行数据（B8 信号库）
signals.db
*.db
*.db-wal
*.db-shm

# 状态快照（B9 仪表盘）
dpb_status.json

# 运行日志
dpb_signals.log
dpb_signals.txt
*.log

# Python
__pycache__/
*.py[cod]
# 虚拟环境：.venv/ 和 venv/ 两种写法都必须挡（本目录实际用的是 venv/）
# ⚠️ 曾经只写了 .venv/ 导致 git add -A 把 3605 个 venv 文件推上 GitHub，
#    事后靠 amend + force-push 才清理掉。改动此处务必两种都保留。
.venv/
venv/

news_calendar.json
dpb_heartbeat.txt
watchdog_state.json
watchdog.log
backtest_cache/
backtest_full.log
backtest_1d.log
backtest_ablate_1h.log
backtest_signif.log
archive/
paxg_fired.json
*.bak
*.bak.*
dpb_config.json.*

# 仪表盘预览图（仅本地，用于汇报）
dashboard_preview.png

# 汇报用预览图（仅本地）
final_preview.png
